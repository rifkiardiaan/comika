# ======================================================================
# COMIKA — Production Environment (InfinityFree)
# Domain: https://comika.free.nf
# ======================================================================

APP_NAME=COMIKA
APP_ENV=production
APP_KEY=base64:NiYJv8hOoMnHWQO4GiJwIyLaK3QdR5LVa0eUiTSyFCU=
APP_DEBUG=false
APP_URL=https://comika.free.nf
FRONTEND_URL=https://comika.free.nf

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Database (InfinityFree MySQL)
DB_CONNECTION=mysql
DB_HOST=sql310.infinityfree.com
DB_PORT=3306
DB_DATABASE=if0_42760113_komika
DB_USERNAME=if0_42760113
DB_PASSWORD=t9nrXR1j21oxcF
DB_STRICT=false

# CORS — izinkan frontend mengakses API
CORS_ALLOWED_ORIGINS=https://comika.free.nf

# Trusted proxies & hosts
TRUSTED_PROXIES=*
TRUSTED_HOSTS=comika.free.nf

# Driver (file-based, kompatibel shared hosting)
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_SECURE_COOKIE=true

# Mail (log only — tidak kirim email asli)
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@comika.free.nf"
MAIL_FROM_NAME="${APP_NAME}"

# Web Push (opsional — kosongkan jika tidak dipakai)
VAPID_SUBJECT=mailto:noreply@comika.free.nf
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# AI Assistant (opsional — kosongkan jika tidak dipakai)
AI_BASE_URL=
AI_API_KEY=
AI_MODEL=gpt-4o-mini
AI_TIMEOUT=60
